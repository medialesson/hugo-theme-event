languages:
  en:
    disabled: false
  de:
    disabled: false

params:
  themes:
    conference:
      conferenceName: Lukas' Conference 2025
      # 👇 Fetch the ID of your conference's API endpoint from
      # 👇 https://sessionize.com/app/organizer/schedule/api/0
      # 👇 and enter it here.
      sessionizeId:
      favicon: "favicon.ico"
      enableSpeakerPages: true
      enableSessionPages: true
