{
    "scripts": {
        "format:check": "prettier --check .",
        "test": "echo \"No test specified yet.\" && exit 1"
    },
    "devDependencies": {
        "prettier": "^3.3.3",
        "prettier-plugin-go-template": "^0.0.15"
    }
}
